#include<stdio.h>
#include<string.h>

void vow_const(char str[], int len) {

    int i = 0, vow = 0, cons = 0;

    for(i; i < len; i++) {
        str[i];
    }

}

void main() {

    char string[] = "Anirudh";
    int length = strlen(string);

    vow_const(string, length);
}